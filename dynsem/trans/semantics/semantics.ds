module semantics/semantics

imports
//  ds-signatures/ds-sig
  semantics/aterm
  semantics/terms
  semantics/module
  
signature
  sorts
    Eval
    Eval2
    
  constructors
    Eval : ATerm * Module -> Eval
    
  arrows
    Eval -init-> (T * Sorts * Constructors * Arrows)
    
rules
  
  Eval(t, spec) -init-> (prog, S, C, A)
  where
    convert(t) --> prog;
    spec :: S {}, C {}, A {} -load-> _ :: S, C, A

  