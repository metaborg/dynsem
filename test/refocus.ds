rules

  e --> e'
  ----------------------------------
  Assign(ref, e) --> Assign(ref, e')
  
  x --> y, Exec(y) --> z
  ----------------------
  Exec(x) --> z
  
  
  a --> b
  --------
  c --Log l --> (d, State s)
  
  a --> b, (Exec(d), State s) --Log lprime --> z
  ----------------------
  c -- Log llprime --> z
  
  
  
  LangCons(x, y, z) = A(x, B(z, C(x, y)))
  
  
  A(x, B(z, C(x, y))) --> e
  -------------------------
  LangCons(x, y, z) --> e
  
  e --> e'
  --------------------
  A(x, e) --> A(x, e')

  x --> x'
  --------------------
  A(x, e) --> A(x', e)
    
/*

  B(z, C(x, y)) --> e'
  --------------------------------
  A(x, B(z, C(x, y))) --> A(x, e')
  --------------------------------
  LangCons(x, y, z) --> 
  

  x --> x'
  ----------------------------------
  A(x, B(z, C(x, y))) --> A(x', B(z, C(x, y)))
  --------------------------------------------
  LangCons(x, y, z) --> LangCons(x', y, z)
  
  
*/
  
  
  
  
  